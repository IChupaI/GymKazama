type GuideListElement = {
  id: string;
  name: string;
  description: string;
};

type CreateGuideListElement = {
  name: string;
  description: string;
};

type DeleteGuideListElement = {
  id: string;
}